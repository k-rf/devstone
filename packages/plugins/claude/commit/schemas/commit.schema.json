{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Commit Plugin 設定",
  "description": "コミットプラグインのプロジェクト固有設定。ヘッダーは `<emoji> <課題キー> <件名>` 形式のみ。",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema への参照"
    },
    "style": {
      "type": "string",
      "enum": ["gitmoji"],
      "default": "gitmoji",
      "description": "コミットメッセージのスタイル。Conventional Commits は廃止済み。"
    },
    "language": {
      "type": "string",
      "default": "en",
      "description": "メッセージの言語（ISO 639-1 コード）",
      "examples": ["en", "ja"]
    }
  },
  "additionalProperties": false
}
