{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Commit Plugin 設定",
  "description": "コミットプラグインのプロジェクト固有設定",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema への参照"
    },
    "style": {
      "type": "string",
      "enum": ["gitmoji", "conventional"],
      "default": "gitmoji",
      "description": "コミットメッセージのスタイル"
    },
    "language": {
      "type": "string",
      "default": "en",
      "description": "メッセージの言語（ISO 639-1 コード）",
      "examples": ["en", "ja"]
    },
    "scope": {
      "type": "boolean",
      "default": false,
      "description": "スコープをメッセージに含めるか（例: feat(auth):）"
    },
    "customPrefix": {
      "type": "string",
      "description": "追加プレフィックスのパターン（例: チケットキー「PROJ-123」）。任意のスタイルと組み合わせて使用できる。",
      "examples": ["PROJ-123", "[ISSUE-456]"]
    }
  },
  "additionalProperties": false
}
