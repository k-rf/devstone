{
  "$schema": "../../../.moon/cache/schemas/project.json",

  "id": "libs-json-canvas-spec",
  "layer": "library",
  "language": "typescript",
  "toolchains": { "default": "node" },
  "tags": ["testable"],

  "project": {
    "title": "libs/json-canvas-spec",
    "description": "json-canvas-specのライブラリパッケージです。"
  },

  "tasks": {
    "test": {
      "command": "pnpm run test",
      "outputs": ["coverage/**"]
    }
  }
}
