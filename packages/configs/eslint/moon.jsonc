{
  "$schema": "../../../.moon/cache/schemas/project.json",

  "id": "configs-eslint",
  "layer": "configuration",
  "language": "typescript",
  "toolchains": { "default": "node" },
  "tags": ["testable"],

  "project": {
    "title": "configs/eslint",
    "description": "ESLintの設定をまとめたパッケージです。"
  },

  "tasks": {
    "test": {
      "command": "pnpm run test",
      "outputs": ["coverage/**"]
    }
  }
}
