{
  "name": "@devstone/configs-typescript",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScriptの設定をまとめたパッケージです。",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "typecheck": "tsgo -p tsconfig.check.json",
    "lint": "eslint --fix --max-warnings 0"
  },
  "devDependencies": {
    "eslint": "10.2.1"
  }
}
