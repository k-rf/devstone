# devstone

自作のライブラリやツールを集めたモノレポです。

## アプリケーション

各アプリケーションの起動方法・環境変数は、各ディレクトリの `README.md` を参照してください。

- [easel](apps/easel/README.md)
- [notion-toggl-bridge](apps/notion-toggl-bridge/README.md)

## 開発の流れ

### 基本方針

タスクランナーとして `moon` を採用しています。
複数パッケージにまたがる操作やワークスペース全体のタスクは `moon` を通じて行い、
個別アプリケーション特有の開発操作（開発サーバーの起動など）は各ディレクトリの npm scripts を使用します。

### 開発・検証

- **初回のビルド**: ワークスペースパッケージのビルド成果物（`dist`）を解決するため、
  クリーンチェックアウト後は先に `moon run :build` を実行してください。
- **全体の品質検証**: `pnpm run check:all` でフォーマット、型チェック、Lint、
  Markdown / スペル検査、knip、テストカバレッジを一括検証できます。
- **個別タスクの実行**: 各タスクは `moon run :<task>`（例: `lint`, `typecheck`, `format`）や
  `package.json` のスクリプトを使用します。

### 開発のヒント

- **キャッシュの活用**: `moon` はタスク結果をキャッシュします。
  キャッシュを無視したい場合は、コマンドに `--no-cache` を付与してください。
- **依存関係の考慮**: `moon` はプロジェクト間の依存関係（`dependsOn`）を解釈し、
  正しい順序でタスクを実行します。
